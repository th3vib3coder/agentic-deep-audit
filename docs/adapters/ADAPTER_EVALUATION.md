# Adapter Evaluation Template

Status: development-time template; no adapter promoted by this file alone.

Sources: adapter candidate metadata, license text, pinned version, read-only command transcript and independent reviewer decision.

Inputs: adapter candidate, target OS, license, version, read-only command, policy, observed output, fallback.

Outputs: human review card paired with `docs/adapters/<tool>/adapter_decision.json`.

Validation: runtime promotion is machine-checked from `adapter_decision.json`; this Markdown is review context only.

## Required Fields

- Adapter id:
- Installability:
- License:
- Version pinning:
- Read-only command:
- Policy:
- Observed output:
- Fallback:
- Decision:
- Reviewer:
- Decision date:
